/**
 * semi             : Line 끝에 semicolon을 넣도록 합니다.
 * bracketSpacing   : {foo:bar} => { foo: bar }
 * arrowParens      : 항상 arrow function의 인자는 괄호로 둘러쌓여 있습니다.
 * singleQuotes     : string 은 singleQuote를 사용합니다.
 */

module.exports = {
  semi: true,
  tabWidth: 2,
  printWidth: 100,
  arrowParens: 'always',
  singleQuote: true,
};
